# OTB settings.
[[ -f ~/.otb ]] && source ~/.otb

export PATH="/usr/local/bin:$PATH:/user/local/lib/node_modules"
export PATH="$HOME/.bin:$PATH"

eval "$(rbenv init - --no-rehash)"

PS1='${SSH_CONNECTION+"%{$fg_bold[green]%}%n@%m:"}%{$fg_bold[blue]%}%c%{$reset_color%}$(git_prompt_info) 🐼  '
