# OTB settings.
[[ -f ~/.otb ]] && source ~/.otb

export PATH="/usr/local/bin:$HOME/.bin:$PATH:/user/local/lib/node_modules"

# load rbenv if available
if which rbenv &>/dev/null ; then
  eval "$(rbenv init - zsh --no-rehash)"
fi
